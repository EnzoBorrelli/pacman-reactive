export enum Direction {
  up = "up",
  down = "down",
  left = "left",
  right = "right",
}

export enum CharacterSize {
  challenge = 12,
  classic = 24,
  small = 36,
}

export enum objectSize {
    challenge = 8,
    classic = 16,
    small = 24,
}
