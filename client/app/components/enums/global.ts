export enum Direction {
  up = "up",
  down = "down",
  left = "left",
  right = "right",
}

export enum CharacterSize {
  challenge = 16,
  classic = 32,
  small = 48,
}

export enum objectSize {
    challenge = 8,
    classic = 16,
    small = 24,
}
