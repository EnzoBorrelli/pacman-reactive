export enum GhostState {
  walking = "walking",
  frightened = "frightened",
  recovering = "recovering",
  idle = "idle",
}

export enum GhostType {
  inky = "inky",
  blinky = "blinky",
  pinky = "pinky",
  clyde = "clyde",
}
